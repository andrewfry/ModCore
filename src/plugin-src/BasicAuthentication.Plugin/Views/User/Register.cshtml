@using ModCore.Core.HtmlHelperExtensions

@{ 
    Layout = Html.DefaultLayout();
}

@model ModCore.ViewModels.Access.vRegister

<div class="panel panel-default">
    <div class="panel-title">
        Register
    </div>
    <div class="panel-body">
        <form action="/Register" method="post" class="form-horizontal">
            @Html.AntiForgeryToken()

            <div class="form-group">
                <div class="col-xs-12">
                    @Html.TextBoxFor(a => a.FirstName, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    @Html.TextBoxFor(a => a.LastName, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    @Html.TextBoxFor(a => a.EmailAddress, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    @Html.PasswordFor(a => a.Password, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    @Html.PasswordFor(a => a.ConfirmPassword, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">Register</button>
                </div>
            </div>

        </form>
    </div>
</div>